import { HttpClientWrapper } from './http-client-wrapper';

describe('HttpClientWrapper', () => {
  it('should create an instance', () => {
    expect(new HttpClientWrapper()).toBeTruthy();
  });
});
